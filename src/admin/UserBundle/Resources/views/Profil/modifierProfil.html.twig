{% extends "::layout.html.twig" %}

{% block title %}{{parent()}} - Ajout profil{% endblock %}

{% block stylesheets %}
    {{parent()}} {#<link rel="stylesheet" type="text/css" href="{{ asset('css/page/login.css') }}" />#}
{% endblock %}
{% block file_arriane %}
     <div class="fond-fil-ariane" >
                    <div class="zone-fil-ariane" style="background:rgba(0,0,0,0.4); width: 100%; height : 0px;padding-top: 0px;color: #fff;">
                        <div class="row" style="background:rgba(0,0,0,0.3)">
                            <div class="col-xs-7" >
                               <h1> Modifier un profil</h1>
                            </div>
                            <div class="col-xs-5" style="margin-top: 15px; ">
                               <span class="fil-1"> Accueil</span> > <span class="fil-2">Gestion des profils</span> ><span class="fil-3"> Modification </span>
                            </div>
                        </div>
                    </div>
		</div>           
{% endblock  %}
{% block body %}


    <div class="col-lg-12">
                {% for contenu in app.session.getflashbag.get('profil.ajout.success')%}
                    {% if loop.index == 0%}
       
            <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12 ">
                            {% endif%}
                <div class="text-center alert alert-success fade in">{{contenu}}</div>
                            {% if loop.index == 0%} 
            </div>
       
                    {% endif%}
                {% endfor%}

                {% for contenu in app.session.getflashbag.get('profil.ajout.error')%}
                    {% if loop.index == 0%}
       
            <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12 ">
                            {% endif%}
                <div class="text-center alert alert-danger fade in">{{contenu}}</div>
                            {% if loop.index == 0%} 
            </div>
       
                    {% endif%}
                {% endfor%}

                {% for contenu in app.session.getflashbag.get('profil.ajout.already.exist')%}
                    {% if loop.index == 0%}
       
            <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
                            {% endif%}
                <div class="text-center alert alert-danger fade in">{{contenu}}</div>
                            {% if loop.index == 0%} 
            </div>
        
                    {% endif%}
                {% endfor%}
   
 </div>


        
            <div class="panel panel-white">
                <div class="panel-body">
                    <div class="col-lg-12">
{#                    <form class="form-horizontal" role="form" action="{{path('app_admin_profil_modifier', {'idProfil' : idProfil})}}" method="POST"  {{form_enctype(formuView)}}>#}
                    {{ form_start(formuView) }}
                    <div class="form-group clearfix">
                            <label class=" control-label">{{ form_label(formuView.nom) }}</label>
                            <div class="control">
                                    {{ form_widget(formuView.nom) }}
                                    {#<p class="help-block">Example block-level help text here.</p>#}
                            </div>
                        </div>
                        <div class="form-group clearfix">
                            <label class=" control-label">{{ form_label(formuView.typeProfil) }}</label>
                            <div class="control">
                                    {{ form_widget(formuView.typeProfil) }}
                                    {#<select class="form-control m-bot15">
                                        <option>Option 1</option>
                                        <option>Option 2</option>
                                        <option>Option 3</option>
                                    </select>#}
                            </div>
                        </div>
                        <div class="form-group">
                            <label  class=" control-label">Description</label>
                            <div class="control">
                                    {{ form_widget(formuView.description) }}
                            </div>
                        </div>                           
                        <div class="form-group clearfix" style="margin-left: 80px;">
                                {{form_rest(formuView) }}
                            <a  class="btn" href="{{path('app_admin_profils_user')}}"> 
                                <i class="icon icon-white icon-arrow-left"></i> Annuler
                            </a>
                            <button type="submit" class="btn btn-success" id="submit" > <i class="icon icon-white icon-ok"></i> Valider </button>
                        </div>                             
                </div>
                {{ form_end(formuView) }}
{#                </form>#}
        </div>
        </div>
    </div>


{% endblock %}
{% block scripts %}
{{parent()}}

{% endblock %}





{% extends "::layoutAuth.html.twig" %}

{% block title %}Connexion{% endblock %}


{% block body %}

<div class="col-md-12" >
 <div class="text-center" >
            {% for mesg in app.session.getflashbag.get('ina') %}
        <div class="alert alert-info fade in">{{mesg}}</div>
            {% endfor %}
            {% for mesg in app.session.getflashbag.get('login.user.lock') %}
        <div class="alert alert-warning fade in">{{mesg}}</div>
            {% endfor %}
            {% for mesg in app.session.getflashbag.get('login.user.already.login') %}
        <div class="alert alert-warning fade in">{{mesg}}</div>
            {% endfor %}
            {% for mesg in app.session.getflashbag.get('login.form.ivalide') %}
        <div class="alert  alert-danger fade in">{{mesg}} </div>
            {% endfor %}
            {% for mesg in app.session.getflashbag.get('logout.success') %}
        <div class="alert alert-success fade in">{{mesg}}</div>
            {% endfor %}
    </div>
     <img src="{{asset('img/schoolSecurite.fw.png')}}" />

</div>
<div class="col-md-12">
   
    <div class="ibox-content">
        <form role="form" action="{{path('app_admin_user_login')}}" method ="POST">
            <div class="form-group">
                <input class="form-control" placeholder="Votre pseudo" required="true"  type="text" name="email" id="email" value="{{email}}" >
            </div>
            <div class="form-group">
                <input class="form-control password" type="password" name="password"  id="password" value="{{password}}" required="true"  placeholder="Mot de passe">
            </div>
            <div class="checkbox">
{#								<label>#}
								{#	<input name="remember" type="checkbox" value="Remember Me">Remember Me
								</label>#}
                                                                    {% for restoreUrl in app.session.getflashbag.get('restoreUrl') %}
                <input type="hidden" name="restoreUrl" value="{{restoreUrl}}" />
        {% endfor %}
                <button type="submit" class="btn btn-primary block full-width m-b">Connexion</button>

               {# <a href="#">
                    <small>Forgot password?</small>
                </a>

                <p class="text-muted text-center">
                    <small>Do not have an account?</small>
                </p>
                <a class="btn btn-sm btn-white btn-block" href="register.html">Create an account</a>#}
        </form>
        <p class="m-t">
            <small>schoolPro &copy; 2016</small>
        </p>
    </div>
</div>
</div>
 




{% endblock %}

{% block javascriptsPerso %}
<script type="text/javascript">
    $(document).ready(function() {
        $('#tel').numeric();
        $('#tel').keyup(function() {
            var saisie = $(this).val();
            if (saisie.length > 0) {
                var number = parseInt(saisie);
                if (typeof number != 'undefined') {
                    var newVal = (number == 0) ? '' : number;
                    $(this).val(newVal);
                }
            }

        });
    });
</script>
{% endblock %}



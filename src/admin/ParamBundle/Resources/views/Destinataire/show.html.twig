{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Destinataire</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>User</th>
                <td>{{ entity.user }}</td>
            </tr>
            <tr>
                <th>Tierprof</th>
                <td>{{ entity.tierProf }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('destinataire') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('destinataire_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
